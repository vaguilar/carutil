use crate::car::CarHeader;
use binrw;

mod car;

pub fn parse() -> CarHeader {
    unimplemented!("");
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        // let result = add(2, 2);
        // assert_eq!(result, 4);
    }
}
